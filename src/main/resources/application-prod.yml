spring:
  datasource:
    url: jdbc:mysql://localhost:3306/movieclub
    username: root
    password: password
  liquibase:
    contexts: prod
  web:
    resources:
      static-locations: classpath:/static/,file:/C:/Users/Dawid/uploads/

app:
  storage:
    location: C:/Users/Dawid/uploads